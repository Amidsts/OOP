/**
 * The Dependency Inversion principle states that classes should depend upon interfaces or abstract classes instead of concrete classes and functions. OR classes shouldn't depend on other classes, but instead should depend on the interfaces that those classes implement.
 */